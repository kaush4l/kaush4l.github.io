# bravo-resume

Next.js (App Router) resume/portfolio site.

## Dev

```bash
npm ci
npm run dev
```

## Static build

```bash
npm run build
```

Build output is exported to `out/` (see `next.config.ts`).
